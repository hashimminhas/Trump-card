apiVersion: v1
kind: Service
metadata:
  name: trump-card-api
  namespace: default
spec:
  selector:
    app: trump-card-api
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP