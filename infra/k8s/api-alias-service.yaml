apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: default
spec:
  selector:
    app: trump-card-api
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP